node_modules/
dist/
dist-ssr/
coverage/
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local scratch / reference bundles (never commit)
*.zip
todo*.txt
todos*.txt

# Claude Code transient worktrees
.claude/worktrees/

.vercel
.env*

# RL dance project — derived chart timelines (thousands of files, regenerable
# via `npm run extract:charts`; never commit)
data/chart-timelines/

# Local dev/verify scratch (not committed)
.tmpbench/
robot-test.html
vrm-test.html
.*.mjs

# Local-only Miku working files (hi-res source, experiments) — kept out of the
# repo for size. The ONE shipped Miku is the optimized, license-compliant
# derivative Miku4.vrm (see public/models/MODELS_LICENSE.md), explicitly allowed.
public/models/Miku*.vrm
!public/models/Miku4.vrm

# Generated DDR RL fleet status feed (written by isaac-spike/experiments_status.py)
public/experiments.json

# --- DDR RL local artifacts: large, regenerable, or experiment scratch (never commit) ---
data/
outputs/
wandb/
*.mp4
# Abandoned in-browser RL dancer model assets (the ?rldancer harness was removed).
public/models/rl-dancer/
# Scratch reference screenshots dropped at the repo root (the shipped ones live in docs/img + public/).
/c9_*.png
/itg_song_select.png
/arrows half filled.png
