# Generated function bundles (scripts/buildApi.mjs) — not hand-written code.
api/*.js

# Binary asset(s) — 3D model files, not code (prettier can't parse them).
*.glb

# Local-only, gitignored tool state — absent from CI's checkout, so ignore it
# here too and the pre-commit/pre-push format check matches CI exactly.
.claude/settings.local.json

# Local scratch (agent/verify tooling), not committed code.
.tmpbench/

# Local dev/verify harness (temp, not shipped)
robot-test.html
.*.mjs

# Machine-generated fleet feed (experiments_status.py rewrites it every 60s).
public/experiments.json

# Machine-generated replay fixture (scripts/genReplayFixture.mjs writes minified JSON).
tests/fixtures/replay-sample.json

# DDR RL local artifacts (gitignored; huge / not source — keep the format check fast + CI-matching).
data/
outputs/
wandb/
public/models/rl-dancer/
